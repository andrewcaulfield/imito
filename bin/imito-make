#!/usr/bin/env node

const program = require('commander'),
     MakeCommand = require('../lib/cmds/make');

program
    .parse(process.argv);

    console.log(new MakeCommand(program).exec());

// new MakeCommand(program).exec();
